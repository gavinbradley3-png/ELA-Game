# Required in production: secret used to sign teacher session cookies.
# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Optional: where the SQLite database lives (defaults to ./data/receipts.db).
# RECEIPTS_DB_PATH=/var/data/receipts.db
